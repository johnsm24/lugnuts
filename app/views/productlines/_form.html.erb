<%= form_with(model: productline, local: true) do |form| %>
  <% if productline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(productline.errors.count, "error") %> prohibited this productline from being saved:</h2>

      <ul>
        <% productline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :textDescription %>
    <%= form.text_field :textDescription %>
  </div>

  <div class="field">
    <%= form.label :htmlDescription %>
    <%= form.text_area :htmlDescription %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
